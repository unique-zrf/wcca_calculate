# Review Record

Model `rc_delay` version `1.0.0` is approved for Phase 1 WCCA automation demos and historical-case regression.

Review requirements before production release:

- Confirm whether downstream threshold input leakage should be modeled.
- Confirm whether propagation delay is handled separately.


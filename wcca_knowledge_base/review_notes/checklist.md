# WCCA Review Checklist

- Confirm each selected model is `approved`.
- Confirm every required parameter has `source` and `review_status`.
- Confirm Typical values are marked `risk_accepted` and listed as risks.
- Confirm Pass/Fail agrees with worst-case limits and requirements.
- Confirm report numbers are generated from CLI result files.

